import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.toebes.rendergltf',
    appName: 'rendergltf',
    webDir: 'dist',
};

export default config;
